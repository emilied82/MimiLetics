import { NgModule } from '@angular/core';
import { IonicModule } from 'ionic-angular';
import { GraphsPage } from './graphs';

@NgModule({
  /*declarations: [
    Graphs,
  ],
  imports: [
    IonicModule.forChild(Graphs),
  ],
  exports: [
    Graphs
  ]*/
})
export class GraphsModule {}
